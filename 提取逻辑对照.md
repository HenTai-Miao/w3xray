# 提取逻辑对照：原版盒子(box) vs 本项目

> 用 systematic-debugging 流程，基于**实证**（box 的 exe/DLL 字符串、游戏数据、逐图测试）
> 梳理出 box 的提取逻辑，并整理对齐到本项目。**未破解/未复制 box 的加壳代码**——
> box 用的是公开的 `jass.slk` 算法，本项目按同一公开思路独立实现。

## 一、box 的提取逻辑（实证来源：box exe 内字符串）

box = `主程序(VMProtect加壳)` + `StormLib.dll`(读MPQ) + `Get.dll`(注入游戏读内存)。
其 exe 字符串暴露出文件提取流水线：

1. 内嵌 **Lua + `jass.slk`** 库（`local slk = require 'jass.slk'`）——这是 YDWE 系开源的
   魔兽对象数据解析库。
2. **加载游戏基础对象库**（从 war3.mpq 等）：
   - `Units\*UnitFunc.txt` / `*UnitStrings.txt`（单位）
   - `Units\ItemFunc.txt` / `ItemStrings.txt`（物品）
   - `Units\*AbilityFunc.txt` / `*AbilityStrings.txt`（技能）
   - `Units\*UpgradeFunc.txt` / `*UpgradeStrings.txt`（科技）
   - `UI\WorldEditStrings.txt` / `WorldEditGameStrings.txt`（WESTRING_ 编辑器字符串）
3. **读取地图文件**：`war3map.w3u/w3t/w3a/w3q` + `war3map.wts` + `war3map.j` + `scripts\war3map.j`
4. **合并**：基础对象库 + 地图修改 → 完整的"基础+自定义"对象表，全部带名/带字段
5. 另有「提取游戏中地图」按钮 = 注入游戏读内存（本项目不做，属外挂）

## 二、本项目的提取流水线（已对齐 box 的文件提取部分）

```
MPQ 读取(mpq.py)               解压(explode.py: PKWARE) + zlib/bzip2
  ├─ HM3W头偏移、表解密、扇区/未压缩、scripts\ 回退、被占用→临时副本
  │
对象数据(api.load_map)
  ├─ 文本对象档(textobj.py)：扫描内部文件，识别 [码]Name=Tip=Ubertip= 的 INI
  │     分类：字段特征(单位有Propernames/Trains，技能有Order/TargetArt，科技R码) + 默认物品
  ├─ 二进制对象(w3obj.py)：w3u/w3t/w3a/w3q/w3b/w3d/w3h（文本已覆盖的类别则跳过，避免无名残留）
  ├─ 名称解析：
  │     · 地图字符串 TRIGSTR_n  → war3map.wts (wts.py)
  │     · 基础对象名/后缀        → base_names.py（游戏 *Strings/*Func 提取，2299条，含 buff 名）
  │     · 编辑器字符串 WESTRING_ → westrings.py（游戏 WorldEditStrings 提取，8065条）
  ├─ 脚本兜底(script_scan.py)：war3map.j 里 $hex/'xxxx' 物品/单位码补缺
  │
图标(icons.py + blp.py)         地图内自定义BLP + 游戏MPQ基础图标(mmap不爆内存)
隐藏指令/合成配方(script_scan.py) 扫描脚本里的聊天指令与物品合成配方
```

## 三、对齐前后的差距（systematic-debugging 找到的根因）

| 差距(根因) | box 怎么做 | 本项目修复 |
|---|---|---|
| 物品列空(分类靠代码首字母I，小写码判错) | jass.slk 按文件/字段定型 | 改"字段特征+默认物品"，14图0空列 |
| 物品/单位无名(只显示代码) | 加载游戏基础库合并 | 文本路径补 base_names 回退 + EditorSuffix + *Func.txt |
| WESTRING_ 常量没解析(可破坏物名) | 加载 WorldEditStrings | 生成 westrings.py 并在名称/字段解析 |
| `scripts\` 子目录脚本漏读 | StormLib 通用 | MPQ 查找回退 scripts\ |
| $hex 物品码漏读 | jass 解析 | 脚本扫描支持 $XXXXXXXX |

## 四、仍存在的边界（如实记录）
- 极少数游戏数据里也无名的系统技能/buff（如 5493 图的 `AIa2` 类）——box 也只能显示码/后缀。
- 「提取游戏中地图」(读内存) 不做。
- 完整"基础对象字段库"（未被地图修改的基础对象的全部默认数值）暂未做——
  当前已覆盖名称；如需基础对象的完整默认字段，可后续把 *Data.slk 也解析进来。
- **加密保护图（如千风物语的 CAME 头 + KKWE 加密 JASS）不支持**：本工具只做普通
  未加密地图的静态提取，不处理 CAME/KKWE 保护头、不导出匿名加密载荷、不做运行时 dump。
  （早期版本曾尝试过千风物语类保护图的静态恢复，现已移除该功能，回归纯粹的正常地图提取器。）

# 批量图标、完整描述与装备关系提取

## Schema 5 当前契约

批量输出使用 schema 5 / extraction revision 5。输入地图、旧输出、旧缓存和客户端证据都是
只读根；新产物只写到显式选择的 v5 输出根。schema 5 把一张地图的结果拆成三条独立轴：

- **发布结果**：是否形成通过所有权、内容清单、必需报告和文件哈希验证的权威发布。
- **档案完整性**：完整、存在原始块、存在损坏块或存在受限块，不因“发布成功”而隐藏缺口。
- **知识证据完整性**：依据当前文本、图标缺口、关系部分和端点未解析单独判断完整或部分。

单一的“完整/部分/受限”状态只作为兼容投影；`三轴状态汇总.tsv` 才是这三条轴的权威视图。

每张地图的 `对象完整描述.tsv` 保留全部文本证据，并用 `是否当前值` 标记当前选择；GUI 可在
「当前文本」与「全部证据」之间切换，不删减原始值。每张地图还必须发布 `图标未解析.tsv`，
保留规范路径、主原因、诊断标志、对象身份和引用集合，发布验证会把这些行与清单和状态计数
逐项对账。

批次根另外发布四份全局证据：

- `图标缺口汇总.tsv`：只聚合通过清单验证的逐地图 `图标未解析.tsv`。
- `图标候选绑定.tsv`：记录跨地图同路径或匿名内容哈希匹配；`是否采用` 必须为“否”。
- `图标缺口统计.txt`：按原因、命名空间、扩展名、对象分类和地图汇总。
- `三轴状态汇总.tsv`：每个输入的发布、档案和知识三轴以及精确计数。

候选只供人工排查，绝不会被采用为当前地图资源，也不会覆盖缺口记录。终止/失败输入只进入
三轴状态，不会凭未验证的地图目录制造全局图标证据。

## 历史 schema 4 验收结果

工具已对 `/Users/zhongerbing/Desktop/Maps/` 中 39 张地图完成 schema 4、revision 4
只读验收。以下路径和数字是 2026-07-15 的历史记录，不是 schema 5 实测结果。最终产物位于：

`/Users/zhongerbing/Documents/xm/war3_xg/map-extract-output-v4/`

- 39/39 已发布，1 张“完整”、38 张“部分完成”，失败与受限结果均为 0。
- 109,637 个对象产生 700,979 条完整文本证据。
- 13,442 条装备关系：8,366 条掉落/获取关系和 5,076 条装备技能关系。
- 79,078 条图标记录全部写出原始文件并成功转换 PNG；物理文件缺失为 0。
- 39 张源地图处理前后的路径、大小、mtime-ns 与 SHA-256 清单逐项一致；历史 v2 输出的
  158,226 个文件也通过内容级 Merkle 前后对比，未被本轮修改。

“部分完成”表示仍存在明确的静态证据缺口，例如缺少客户端文本、同优先级文本冲突或具名图标
文件无法证明；它不表示已发布文件写出失败。

## 运行命令

```bash
uv run main.py description-cache migrate \
  --legacy-output <schema-1-root> \
  --legacy-cache <schema-2-cache.tsv> \
  --output <owned-cache-root>

uv run main.py batch <maps-root> \
  --output <v5-root> \
  --game-data <client-or-trusted-icon-root> \
  --description-cache <owned-cache-root>

uv run main.py integrity snapshot \
  --root maps=<maps-root> \
  --root legacy-v1=<root> \
  --root legacy-v2=<root> \
  --root legacy-v4=<root> \
  --output <before.json>

uv run main.py integrity verify --snapshot <before.json>
uv run main.py integrity retained-cache \
  --active-root <owned-cache-root> \
  --output <retained-cache-report.json>
```

`--game-data` 可指向可读的 Warcraft 客户端数据目录。经典客户端按
`War3Patch.mpq → War3xLocal.mpq → War3x.mpq → war3.mpq` 查询。历史 schema 4 验收使用由旧批次客户端
MPQ 证据构建的只读可信图标缓存；缓存中的每个虚拟路径都绑定 SHA-256、原客户端档案和同一张
源地图的引用证据，不能把其他地图的自定义图标当作客户端资源。

schema 5 只接受显式 `--description-cache` 指向的已拥有、已验证缓存 generation。迁移命令从
只读 legacy 输入构建新缓存；批处理不会从任意旧结果隐式采纳文本。GUI 也可从数据工具菜单
选择或清除可信描述缓存。缓存只接纳原版基础对象、客户端补全、键和值均唯一的记录。

`integrity snapshot` 与 `verify` 用内容和纳秒 mtime 证明只读输入前后一致。`retained-cache` 将
有意保留的 `previous`、失败 stage/output 和 recovery，与仍在发布命名空间中的 stage/backup
临时违规分开报告；它不会把保留对象加载为描述来源，也不会删除、修复或重命名它们。
完整性报告覆盖采用追加式不可变历史：新报告保持 `--output` 原路径，旧报告移入
`.w3xray-integrity-history/<输出文件名 SHA-256>/<generation-id>/`。历史代的
`report.json` 由 `历史清单.json` 绑定原文件名、大小和 SHA-256，每次覆盖只追加一代，
不会自动删除旧代。历史根名称及其目标文件系统物理别名不能作为报告输出名；交换前失败
会对仍属本次写出的 stage 做身份门控清理，交换结果不可信时保留证据。该历史策略不影响
通用地图/资源写出器的原子替换职责。

## 输出结构

```text
v5-root/
├── .w3xray-global/
│   ├── current.json
│   └── generations/<generation-id>/
│       ├── 全局清单.json
│       ├── 图标缺口汇总.tsv
│       ├── 图标候选绑定.tsv
│       ├── 图标缺口统计.txt
│       └── 三轴状态汇总.tsv
├── 批量提取汇总.tsv
├── 批量提取状态.json
├── 失败与重试.tsv
├── 图标缺口汇总.tsv
├── 图标候选绑定.tsv
├── 图标缺口统计.txt
├── 三轴状态汇总.tsv
└── 地图/
    └── 001_地图名_SHA前8位/
        ├── .w3xray-batch-owned
        ├── 地图摘要.txt
        ├── 图标索引.tsv
        ├── 图标未解析.tsv
        ├── 图标完整性.txt
        ├── 对象描述.tsv
        ├── 对象完整描述.tsv
        ├── 描述完整性.txt
        ├── 掉落与获取关系.tsv
        ├── 装备技能关系.tsv
        ├── 关系完整性.txt
        └── 图标/
            ├── 原始/具名/
            ├── 原始/匿名/
            ├── PNG/具名/
            └── PNG/匿名/
```

每张地图先写入工具拥有的私有暂存目录，全部必需报告和所有权标记齐备后再原子发布。旧 schema
结果不会被 schema 5 断点续跑误认成完成结果；恢复只信任 `current.json` 指向且通过全局清单
验证的不可变 generation。完整批次发布最终权威状态后，会清理同源 SHA 的旧显示名目录，但只
删除严格通过所有权、清单、报告和文件哈希验证且未被当前状态引用的 `地图/` 直接子目录；
未证明所有权的目录、不同源目录、符号链接和中断批次均保持不动。
同一输出根同时只允许一个进程持有跨进程租约；退休候选先原子移入私有 quarantine，再通过目录
描述符、inode、当前 generation、输出租约和 manifest 二次验证后删除，任何证明不足都 fail closed。

## 完整描述规则

`对象完整描述.tsv` 一行对应一个对象、文本角色、等级/变体和证据值。覆盖名称、称谓、编辑器
后缀、普通提示、扩展提示、学习提示、学习扩展提示、关闭提示、关闭扩展提示、Buff/效果提示、
复活提示、唤醒提示、编辑器描述，以及字段元数据确认的其他文本说明。所有等级逐条保留。

每条记录同时保存：

- 原始全文：保留 Warcraft 颜色代码、`|n`、制表符、CR/LF、引号、首尾空白，不限制长度。
- 可读全文：只执行确定性的颜色代码清理和换行展开，不覆盖原始值。
- 来源类型、来源文件或 WTS 字符串位置、字段键、字段标签、等级和证据序号。
- 状态、占位标志和冲突组；同优先级冲突保留全部原值。

解析顺序固定为：

1. 当前地图/战役自己的二进制对象、具名文本、SLK 和 WTS 正文。
2. 当前地图中通过静态验证的匿名文本对象块。
3. 当前 Warcraft 客户端基础对象数据。
4. 已验证且无冲突的原版基础对象可信缓存。

地图中显式零长度值会阻止低优先级补全。固定占位值会保留为证据，但允许客户端或缓存补全。
绝不使用其他地图的自定义对象说明，也不根据名称或短文本猜写描述。

历史 schema 4 的 700,979 条文本证据按七种状态统计如下；这些数量没有重写为 schema 5 结果：

| 状态 | 数量 |
|---|---:|
| 地图原值 | 285,565 |
| 客户端补全 | 0 |
| 可信缓存补全 | 0 |
| 作者明确清空 | 857 |
| 作者未定义 | 7,000 |
| 源数据不可用 | 351,923 |
| 来源冲突 | 55,634 |

本机当前没有完整经典 MPQ/CASC 文本源，验收使用的可信缓存只包含图标证据，不能供应描述。
旧 v2 描述缓存没有 schema-4 要求的来源清单 SHA-256 绑定，因此不会被提升为可信文本证据；
缺少来源的角色保持“源数据不可用”，不跨地图借用自定义说明。TSV 使用标准制表符引用规则；
含颜色码、物理换行、制表符和引号的原始文本经标准 `csv` 解析及安全前缀解码后逐字一致。
可能被电子表格解释为公式的 `= + - @` 开头单元格（含前置空白或控制字符）会在物理 TSV 中
增加可逆的单引号 + U+2060 安全前缀；可信读取器移除该前缀后恢复逐字原值。
权威 39 图审计中最长原始全文为 6,363 字符，341 条记录包含物理换行，128,340 条记录保留
Warcraft 颜色代码证据，均未因 GUI 或 TSV 导出被截断。

## 掉落、获取与装备技能

同一份不可变关系索引同时服务对象详情、独立“掉落/获取”页、知识包和批量 TSV：

- 怪物详情显示直接掉落装备、掉落组、组内序号、原始概率、实例、玩家和坐标。
- 装备详情显示所有获取方式、合成材料、装备技能、共享冷却技能和证据位置。
- 技能详情可反查由哪些装备提供。
- 独立页面支持全文搜索、关系类型/可信度筛选、完整证据查看和源/目标对象跳转。
- 所有正文控件可滚动并支持复制全文，不使用预览切片或省略号。
- 战役子地图使用 `(分类, rawcode)` 精确继承战役共享对象；跨分类同码不会串用名称、字段或图标。

最终关系分类如下：

| 关系类型 | 数量 |
|---|---:|
| 怪物直接掉落 | 135 |
| 可破坏物掉落 | 221 |
| 商店出售 | 4,067 |
| 商店制造 | 1,078 |
| 合成 | 294 |
| 地面预放置 | 277 |
| 预放置背包 | 39 |
| 脚本/触发奖励 | 2,255 |
| 装备技能 | 3,449 |
| 共享冷却技能 | 1,627 |

可信度为 12,836 条“已确认”、606 条“可推断”、0 条“仅线索”。完整性为 7,974 条“完整”、
4,883 条“部分”、585 条“未解析”、0 条“冲突”。无法静态证明事件、接收者或动态变量时只保留
可推断关系或线索，不把任意 `CreateItem` 调用写成怪物必掉。

`war3mapUnits.doo` 与 `war3map.doo` 会保留嵌套掉落组和原始概率。版本 8 固定单位使用的
`randomFlag=-1` 也已覆盖；真实 `LT科技转职` 地图从 0/345 恢复为 345/345 个预放置实例，新增
46 条直接掉落、28 条地面物品和 37 条预放置背包关系。

## 历史 schema 4 图标覆盖口径

- 具名逻辑记录 32,690 条，匿名 BLP 46,388 条，总计 79,078 条。
- 原始写出与 PNG 成功均为 79,078；唯一原始文件和唯一 PNG 各 78,916 份。
- 162 条逻辑记录差来自 `.tga`/`.blp` 引用解析到同一 SHA-256 文件，索引保留两份引用证据，
  磁盘只保存一份内容。
- 9,682 个具名引用没有可证明的地图或客户端文件，继续明确标记为未解析，不绑定匿名块。

最终物理审计逐个核对了索引路径、原始文件 SHA-256、PNG 魔数、符号链接和磁盘文件集合：
79,078 条逻辑记录对应 78,916 份唯一原始文件与 78,916 份唯一 PNG，全部通过。

## 历史 schema 4 最终验证

- 修订 4 全量批处理：39/39 均为 `processed`，耗时 566.45 秒，实测峰值常驻内存
  783.98 MiB，发布文件 5,833,248,359 字节；修订 3 结果未被复用。
- 随后的相同参数验证为 39/39 `reused`，耗时 39.77 秒；`地图/` 与权威状态均精确为 39，
  stage/backup/transaction/quarantine 残留为 0。
- 质量门：150 个维护路径通过 Ruff、格式检查和 basedpyright 0 错误/警告；76 个改动 Python
  文件通过 no-excuse 规则审计，0 个文件超过 250 纯代码行。
- 功能聚焦测试：241 passed。
- 完整回归：1,542 passed、11 skipped、1 subtest passed。
- 真实地图 schema-4 验收：7 passed；旧批次所有非占位原文均按
  分类、对象 ID、合理等级和文本角色绑定到新证据，不用全图同文案替代。
- 39 张源地图处理前后 SHA-256、大小和 mtime-ns 清单完全一致；历史 v2 的内容与元数据聚合
  SHA-256 也完全一致。

## 静态边界

- 动态随机池变更、跨函数变量传播、运行时创建、混淆或加密逻辑可能只能形成部分关系或线索。
- 缺少客户端数据且可信缓存无对应原版键时，文本只能标为“源数据不可用”，不能凭经验补写。
- 匿名图标块没有可验证名称时只按块编号和内容哈希导出，不猜测原路径或对象关系。
- 不执行地图脚本、历史 EXE 或 DLL，不加载 `Game.dll`，不读取进程内存，不做运行时解密，也不
  修改或重新打包源地图。

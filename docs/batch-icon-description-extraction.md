# 批量图标、完整描述与装备关系提取

## 当前验收结果

工具已对 `/Users/zhongerbing/Desktop/Maps/` 中 39 张地图完成 schema v2
只读验收。最终产物位于：

`/Users/zhongerbing/Documents/xm/war3_xg/map-extract-output-v2/`

- 39/39 已发布，1 张“完整”、38 张“部分完成”，失败与受限结果均为 0。
- 122,075 个对象产生 766,396 条完整文本证据。
- 13,047 条装备关系：8,246 条掉落/获取关系和 4,801 条装备技能关系。
- 79,078 条图标记录全部写出原始文件并成功转换 PNG；物理文件缺失为 0。
- 39 张源地图处理前后的路径与 SHA-256 清单逐字一致。

“部分完成”表示仍存在明确的静态证据缺口，例如缺少客户端文本、同优先级文本冲突或具名图标
文件无法证明；它不表示已发布文件写出失败。

## 运行命令

```bash
uv run main.py batch \
  '/Users/zhongerbing/Desktop/Maps' \
  --output '/Users/zhongerbing/Documents/xm/war3_xg/map-extract-output-v2' \
  --game-data '/path/to/Warcraft III'
```

`--game-data` 可指向可读的 Warcraft 客户端数据目录。经典客户端按
`War3Patch.mpq → War3xLocal.mpq → War3x.mpq → war3.mpq` 查询。本次验收使用由旧批次客户端
MPQ 证据构建的只读可信图标缓存；缓存中的每个虚拟路径都绑定 SHA-256、原客户端档案和同一张
源地图的引用证据，不能把其他地图的自定义图标当作客户端资源。

批处理会自动从自己拥有且 schema 可识别的旧结果构建 `可信描述缓存.tsv`。GUI 也可从数据工具
菜单选择或清除可信描述缓存。缓存只接纳原版基础对象、客户端补全、键和值均唯一的记录。

## 输出结构

```text
map-extract-output-v2/
├── 批量提取汇总.tsv
├── 批量提取状态.json
├── 失败与重试.tsv
├── 可信描述缓存.tsv
└── 地图/
    └── 001_地图名_SHA前8位/
        ├── .w3xray-batch-owned
        ├── 地图摘要.txt
        ├── 图标索引.tsv
        ├── 图标完整性.txt
        ├── 对象描述.tsv
        ├── 对象完整描述.tsv
        ├── 描述完整性.txt
        ├── 掉落与获取关系.tsv
        ├── 装备技能关系.tsv
        ├── 关系完整性.txt
        └── 图标/
            ├── 原始/具名/
            ├── 原始/匿名/
            ├── PNG/具名/
            └── PNG/匿名/
```

每张地图先写入工具拥有的私有暂存目录，全部必需报告和所有权标记齐备后再原子发布。schema 1
结果不会被 schema 2 断点续跑误认成完成结果。

## 完整描述规则

`对象完整描述.tsv` 一行对应一个对象、文本角色、等级/变体和证据值。覆盖名称、称谓、编辑器
后缀、普通提示、扩展提示、学习提示、学习扩展提示、关闭提示、关闭扩展提示、Buff/效果提示、
复活提示、唤醒提示、编辑器描述，以及字段元数据确认的其他文本说明。所有等级逐条保留。

每条记录同时保存：

- 原始全文：保留 Warcraft 颜色代码、`|n`、制表符、CR/LF、引号、首尾空白，不限制长度。
- 可读全文：只执行确定性的颜色代码清理和换行展开，不覆盖原始值。
- 来源类型、来源文件或 WTS 字符串位置、字段键、字段标签、等级和证据序号。
- 状态、占位标志和冲突组；同优先级冲突保留全部原值。

解析顺序固定为：

1. 当前地图/战役自己的二进制对象、具名文本、SLK 和 WTS 正文。
2. 当前地图中通过静态验证的匿名文本对象块。
3. 当前 Warcraft 客户端基础对象数据。
4. 已验证且无冲突的原版基础对象可信缓存。

地图中显式零长度值会阻止低优先级补全。固定占位值会保留为证据，但允许客户端或缓存补全。
绝不使用其他地图的自定义对象说明，也不根据名称或短文本猜写描述。

最终 766,396 条文本证据按七种状态统计如下：

| 状态 | 数量 |
|---|---:|
| 地图原值 | 284,478 |
| 客户端补全 | 0 |
| 可信缓存补全 | 56,972 |
| 作者明确清空 | 644 |
| 作者未定义 | 0 |
| 源数据不可用 | 369,006 |
| 来源冲突 | 55,296 |

本次可信缓存包含 3,043 条记录、0 个冲突、0 条诊断。连续重建后条数和文件 SHA-256 均保持
不变。TSV 使用标准制表符引用规则；含颜色码、物理换行、制表符和引号的原始文本经 Python
`csv` 往返后逐字一致。

## 掉落、获取与装备技能

同一份不可变关系索引同时服务对象详情、独立“掉落/获取”页、知识包和批量 TSV：

- 怪物详情显示直接掉落装备、掉落组、组内序号、原始概率、实例、玩家和坐标。
- 装备详情显示所有获取方式、合成材料、装备技能、共享冷却技能和证据位置。
- 技能详情可反查由哪些装备提供。
- 独立页面支持全文搜索、关系类型/可信度筛选、完整证据查看和源/目标对象跳转。
- 所有正文控件可滚动并支持复制全文，不使用预览切片或省略号。

最终关系分类如下：

| 关系类型 | 数量 |
|---|---:|
| 怪物直接掉落 | 135 |
| 可破坏物掉落 | 221 |
| 商店出售 | 3,962 |
| 商店制造 | 1,063 |
| 合成 | 294 |
| 地面预放置 | 277 |
| 预放置背包 | 39 |
| 脚本/触发奖励 | 2,255 |
| 装备技能 | 3,174 |
| 共享冷却技能 | 1,627 |

可信度为 12,441 条“已确认”、606 条“可推断”、0 条“仅线索”。完整性为 8,166 条“完整”、
4,822 条“部分”、59 条“未解析”、0 条“冲突”。无法静态证明事件、接收者或动态变量时只保留
可推断关系或线索，不把任意 `CreateItem` 调用写成怪物必掉。

`war3mapUnits.doo` 与 `war3map.doo` 会保留嵌套掉落组和原始概率。版本 8 固定单位使用的
`randomFlag=-1` 也已覆盖；真实 `LT科技转职` 地图从 0/345 恢复为 345/345 个预放置实例，新增
46 条直接掉落、28 条地面物品和 37 条预放置背包关系。

## 图标覆盖口径

- 具名逻辑记录 32,690 条，匿名 BLP 46,388 条，总计 79,078 条。
- 原始写出与 PNG 成功均为 79,078；唯一原始文件和唯一 PNG 各 78,916 份。
- 162 条逻辑记录差来自 `.tga`/`.blp` 引用解析到同一 SHA-256 文件，索引保留两份引用证据，
  磁盘只保存一份内容。
- 9,682 个具名引用没有可证明的地图或客户端文件，继续明确标记为未解析，不绑定匿名块。

最终物理审计逐个核对了索引路径、原始文件 SHA-256、PNG 魔数、符号链接和磁盘文件集合：
79,078 条逻辑记录对应 78,916 份唯一原始文件与 78,916 份唯一 PNG，全部通过。

## 最终验证

- 最终全量批处理：464.02 秒，峰值常驻内存 887,095,296 字节，输出约 5.7 GiB。
- 功能聚焦测试：103 passed。
- 完整回归：1,384 passed、11 skipped、1 subtest passed。
- 真实地图 schema v2 验收：7 passed，身份规则单测 9 passed；旧批次所有非占位原文均按
  分类、对象 ID、合理等级和文本角色绑定到新证据，不用全图同文案替代。
- 39 张源地图处理前后 SHA-256 清单完全一致。

## 静态边界

- 动态随机池变更、跨函数变量传播、运行时创建、混淆或加密逻辑可能只能形成部分关系或线索。
- 缺少客户端数据且可信缓存无对应原版键时，文本只能标为“源数据不可用”，不能凭经验补写。
- 匿名图标块没有可验证名称时只按块编号和内容哈希导出，不猜测原路径或对象关系。
- 不执行地图脚本、历史 EXE 或 DLL，不加载 `Game.dll`，不读取进程内存，不做运行时解密，也不
  修改或重新打包源地图。

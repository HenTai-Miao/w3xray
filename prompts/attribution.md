你是 w3xtool（一个纯 Python 的魔兽争霸 III 地图解析工具）的解析质量审稿人。
下面是工具对某张地图的**解析诊断报告**。请结合本仓库的解析源码（`w3xtool/` 下的
`mpq.py`/`w3obj.py`/`api.py`/`script_scan.py`/`wts.py`/`huffman.py` 等）判断：工具是否
**缺斤少两**（该解析出来的内容没解析出来），并给出可落地的修复。

## 诊断报告
{report}

## 你的任务
1. **归因**：针对报告里的每条"疑似缺口"（文件在档但 0 对象、名字解析率低、解析告警、
   乱码等），判断是真问题还是正常现象（如装饰物本就多为无名底图、加密保护图无法解析）。
   说清你的依据。
2. **修复**：对确认是真问题的项，给出**具体的代码改动**（unified diff 或明确的函数/行级
   修改说明），并说明为什么这样改、可能的副作用。优先小而精的外科式改动，不要大重构。
3. **验证**：说明该如何验证修复（跑哪些测试 / 用哪类地图）。新增逻辑应当配单元测试。
4. **文档**：如果修复值得记进文档，给出 README / task_plan.md 的补充草稿。

## 硬约束
- 只读分析 + 提建议。**不要**直接提交或推送。改动由人工审阅后应用，并必须通过现有测试。
- 不臆造格式细节；不确定就标注"需要核实"，并指出核实方法（如查 StormLib / mdx-m3-viewer）。
- 不把正常现象当 bug 硬凹（例如真加密保护图、源文件本身就已损坏的乱码）。
